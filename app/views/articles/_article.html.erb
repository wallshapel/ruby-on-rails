<!-- ES IMPORTANTE QUE ESTE ARCHIVO SE LLAME `_article`, ya que representa un artículo de una colección -->
<div class="article" style="padding: 15px; max-width: 300px;">
	<%= article.id %>
	<h3><%= article.title %></h3>
	<p class="text-secondary"><%= article.created_at %></p>
	<%= link_to "Details", article %> <!-- Solo con `article`, `link_to` ya sabe que debe dirigirse al detalle de dicho articulo -->
</div>